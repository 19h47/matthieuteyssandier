{#
 # Video
 #
 # @package WordPress
 # @subpackage MatthieuTeyssandier
 #}

{% if video.url %}

	<video
		class="{{ classes ? classes | join( ' ' ) : '' }}"
		muted
		playsInline
		autoplay
		loop
		src="{{ video.url }}"
	/>

{% endif %}
